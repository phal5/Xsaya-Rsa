# Xsaya-Rsa

런타임에 2D와 3D를 오가는 2.5D 액션 게임.

- **Unity 6000.3.17f1** / URP
- 렌더 애셋: `Assets/Settings/PC_RPAsset.asset` → `PC_Renderer.asset`

---

## 이 문서를 쓰는 법

**담당한 파트를 각자 자기 절에 적는다.** 남의 절을 고치지 않는다 — 무엇을 왜 그렇게 두었는지는 그것을 만든 사람만 안다.

적을 것은 *무엇을 만들었나*보다 **왜 그렇게 두었나**다. 코드를 읽으면 전자는 알 수 있지만 후자는 알 수 없고, 그 모르는 것이 나중에 "쓸모없어 보이는 것"을 지우게 만든다.

---

## 씬 구성

| 씬 | 언제 올라오나 |
|---|---|
| `Parts/Character` | **한 번 올라오면 내려가지 않는다.** 주인공·매니저가 여기 산다 |
| `Parts/UI - General` | 상주. 캔버스·대사·HUD |
| `Parts/Backgrounds/*` | 스테이지마다 교체된다 |
| `Title` | 시작 화면. 게임에 들어가며 내려간다 |

**배경 씬은 주인공을 인스펙터로 꽂을 수 없다.** 씬을 넘는 참조는 유니티가 저장하지 못하기 때문이다 — 꽂아둔 것처럼 보여도 로드되면 비어 있다. 그래서 배경 쪽이 `PlayerManager.instance`·`SceneDirector.instance`·`FlipBook.Instance`처럼 **런타임에 찾아 쓴다.** 반대 방향(캐릭터 → 배경)도 마찬가지다.

---

## 상태 기계 · 주인공 <sub>담당: phlnxciws5</sub>

### 뼈대

`FiniteStateMachine`은 `MonoBehaviour`이면서 스스로 `IState`다. 그래서 **상태 기계를 상태 자리에 끼워 넣을 수 있고**, 끼워 넣은 것이 곧 그 부모의 초기 상태가 된다. 하위 머신은 `enabled = false`로 두고 루트 하나만 유니티의 `Update`/`FixedUpdate`를 받는다.

```
CharacterRoot
 ├ Character_Controlled ── Character_Ground   ── Ground_Idle / Move / Land / Dodge
 │                       ├ Character_Airborne ── Airborne_Move / Fall / Dodge
 │                       ├ Character_Ledge    ── Ledge_Catch / Hang / Climb / Release / WallJump
 │                       ├ Character_Execution (스킬)
 │                       └ Character_Interact / Heal / Swap
 ├ Character_Hit / Stun
 ├ Character_Down  (죽음)  ·  Character_Faint (가사 — 자세는 같고 스스로 깨어나지 않는다)
 ├ Character_Rest  (누웠다 일어나기)
 └ Character_UI
```

### 여기서 자주 발이 걸리는 것

**`Time.timeScale`이 0이면 `FixedUpdate`가 아예 돌지 않는다.** 전이를 `FixedUpdateState`에만 걸어두면 시간이 멎은 구간에서 영영 빠져나오지 못한다. 시간을 눌러두는 연출(`Character_Rest`)이 있으므로, 그런 상태의 전이는 `UpdateState`에 둔다.

**`Transitions()`는 `Update`와 `FixedUpdate` 양쪽에서 돈다.** 한 프레임에 여러 번 도는 것을 전제로 써야 한다.

**부모가 돌아오면 하위 상태의 `Enter()`가 다시 불린다.** 떠났던 자리에서 재개하려는 설계다. 그래서 `Enter()`에 되돌릴 수 없는 부작용(상호작용 발동, 자원 소모)을 두면 **재개할 때마다 그 부작용이 되풀이된다.** 이어서 하면 안 되는 상태는 `ResumesOnReturn => false`로 표시한다.

**`Rigidbody.position`에 적는 것은 물리 자세일 뿐이다.** `ProjectSettings/DynamicsManager`의 `m_AutoSyncTransforms`가 꺼져 있어, 적어둔 값이 `Transform`으로 내려오는 것은 **다음 시뮬레이션 스텝**이다. 그런데 `Time.timeScale`이 0이면 그 스텝이 아예 오지 않는다 — 옮겨놓고도 화면에서는 아무 일도 일어나지 않고, 시간이 풀리는 순간 뒤늦게 순간이동한다. 시간이 멎을 수 있는 자리(`Pin`, `SceneDirector.Place`, `Dimensional.Flatten`)에서는 **둘 다 적는다.**

**애니메이터에 거는 것은 부탁이지 명령이 아니다.** `Animation.Play`/`CrossFade`는 애니메이터가 *다음에 돌 때* 반영된다. 시간이 멎어 있거나 저프레임이면 그 "다음"이 오지 않아 직전 자세가 그대로 남는다. 그 자리를 위해 `Character_Animation.Snap()`이 있다 — 걸고 나서 `Animator.Update(0)`으로 그 자리에서 평가시킨다.

### 스킬

`Character_Execution` 아래에서 돈다. 연속 베기는 상태를 여럿 만들지 않고 `SwingStrike[]` 배열의 색인 하나로 다룬다 — 상태로 나눴다면 전이가 동작 수만큼 늘어나는데 그 전이들이 할 일은 전부 "다음 클립을 걸고 시간을 다시 센다"뿐이다.

- 프레임 수는 **클립 프레임**으로 적는다. 클립을 프레임 단위로 들여다보며 고르므로 초로 환산해 적어두면 볼 때마다 다시 나눠야 한다.
- `CrossFadeInFixedTime`의 `fixedTimeOffset`은 **클립 시간이 아니라 상태 시간**이다. 배속으로 틀 때는 배속으로 나눠 넘겨야 한다.
- 궤적 이펙트는 **뿌리는 시점은 휘두르는 순간, 서는 자리는 칼이 지나간 끝**이다. 그 끝자리를 런타임에 알려면 클립을 앞질러 샘플링해야 해서, 미리 재어 `effectPlace`/`effectFacing`에 적어둔다. **무기 계층을 바꾸면 이 값들이 조용히 낡는다.**

### 턱 잡기 (Ledge)

붙잡는 순간 `LedgeGrab.Probe`(공중) 또는 `ProbeFromTop`(가장자리에서 내려가기)이 앵커를 한 번 재고, 매달린 동안 **다시 재지 않는다.**

```
hang  = 벽면 + wallOffset,  윗면 - hangDrop   ← 매달릴 자리
stand = 벽면 - standInset,  윗면              ← 올라서서 설 자리
```

두 자리가 같은 `topHeight`에서 나오므로 **`stand.y - hang.y`는 언제나 `hangDrop`으로 고정**이고, 수평 이동 거리도 자세당 상수다(Braced 0.590 / Freehang 0.352). 서는 깊이의 유일한 주인은 `standInset`이다.

붙어 있는 동안 두 몸은 `Detach(true)`로 키네마틱이고 자리는 `Pin()`이 **직접 대입**한다. 스윕도 충돌도 없는 순간이동이므로, **이 구간의 자리 계산이 틀리면 바닥이 막아주지 않는다.**

최근에 고친 것 셋 — 전부 같은 뿌리에서 나왔다.

1. **저프레임에서 바닥을 뚫고 낙하.** `Fit()`이 아직 전환되지 않은 애니메이터에서 *직전 클립*의 `travel.y`를 읽어 늘리기 배율의 부호가 뒤집혔다. 붙잡기 클립은 travel이 음수, 올라서기는 양수라 올라가기가 그대로 내려가기가 됐다. → 애니메이터가 **요청한 클립을 실제로 잡았는지** 확인한 뒤에만 배율을 낸다.
2. **턱을 넘는 순간의 순간이동.** 앞으로 가는 몫을 넘기 전까지 쌓아뒀다 한 스텝에 통째로 풀었다(0.17~0.59m). → 수평을 클립에서 떼어내 **앵커 기하**로 몬다. 상수 거리를 클립이 대신 잴 이유가 없다.
3. **등반 첫 스텝의 평면 스냅.** 문 자리와 매달릴 평면의 차이를 한 스텝에 지웠다. → `settleSpeed`로 수렴 속도를 제한한다.

위에서 내려오는 경로는 `SettleDown()`이 따로 맡는다. 수평을 **목표에서 되짚어** 높이가 간 만큼만 들어가므로, 수평과 높이가 언제나 함께 도착한다.

### 회복 · 재화 · 상점

- `Character_Heal` — 회복 키를 **붙들고 있어야** 낫는다. 체력이 오르는 것도 횟수가 깎이는 것도 클립의 정해진 프레임(`HealFrame`)이고, 그 전에 놓으면 아무 대가도 치르지 않는다.
- `FinanceManager` — `TimeManager`와 같은 **순수 static 클래스**. 씬에 놓을 오브젝트가 없다. 재화는 스테이지를 넘어 이어져야 하는데 씬 오브젝트로 두면 그 씬이 내려갈 때 사라지거나 다음 씬에서 하나 더 생긴다. `Spend()`는 검사와 차감이 한 연산이다.
- `Bounty` — 잡히면 재화를 남긴다. 인스펙터 배선 대신 `Awake`에서 스스로 `onDeath`에 붙는다. 프리팹을 복제하다 하나쯤 안 꽂힌 것이 생기면 그건 화면에 아무 표시도 남기지 않는다.
- `HealChargePurchase` — 상점의 판매대. 대사도 직접 띄우지 않고 결과마다 `BookHolder`를 하나씩 가리킨다. 대사를 화면에 올리는 길은 `FlipBook` 하나여야 한다.

---

## 적 · 보스 <sub>담당: phlnxciws5</sub>

### 뼈대

```
BossRoot
 ├ Boss_Idle              잠들어 있다. 깨어나며 "싸울 화면"을 정한다
 ├ Boss_Alert ── Boss_MoveMachine     ── Boss_Approach / Boss_Retreat
 │             ├ Boss_SkillMachine    ── Boss_Skill / Boss_JumpSmash / Boss_Recite
 │             └ Boss_ReactionMachine ── Boss_Dodge / Boss_Guard / Boss_Sidestep
 ├ Boss_DamageMachine ── Boss_Staggered
 ├ Boss_Dead              시체가 남는다
 └ Boss_Rise              연출용. 들어오면 나가지 않는다
```

피격과 사망 구독은 `BossRoot` 한 곳에 있다. 어느 상태에서든 끊고 들어와야 하는 것을 하위 머신마다 걸면, 상태를 하나 만들 때마다 그 두 줄을 잊을 자리가 하나씩 는다.

회피와 방어는 **진입 조건이 "플레이어의 공격을 감지"로 같다.** 그래서 판정은 `Boss_Alert`에서 한 번만 하고, 둘 중 무엇을 할지는 `Boss_ReactionMachine`이 고른다.

### 영구 죽음 — 기억이 씬 밖에 있어야 하는 이유

보스는 **배경 씬에 놓인 인스턴스**다. 플레이어가 죽어 부활하면 `SceneDirector`가 배경을 통째로 내렸다 다시 부르므로, 죽여둔 보스도 새 오브젝트로 태어난다. 그래서 명부(`BossGraveyard`)는 씬 밖 static이고, `BossGrave`가 `Awake`에서 자기 이름표를 물어 이미 묻혔으면 스스로 사라진다.

- **한 판까지다.** 직렬화하지 않으므로 앱을 껐다 켜면 비어 있다 — 체크포인트와 같은 규칙이다. 세이브가 생기면 적어야 할 것은 이 집합 하나뿐이다.
- **빈 이름표는 묻지 않는다.** 빈 문자열 하나가 명부에 들어가면 `bossId`를 적지 않은 보스가 **전부 함께** 사라진다. 대신 경고를 남긴다.
- `Tools/보스 명부`로 들여다보고 비운다. 하나만 되살리는 것은 `Exhume(id)`다.

### 화면 모드는 보스가 정한다

`BossCombatView`가 `Keep`/`Flat2D`/`Full3D` 셋인 이유는 **"화면을 바꾸지 않는 보스"와 "2D로 바꾸는 보스"가 다르기 때문**이다. bool 하나로 뭉개면 3D 스테이지에 놓인 중간보스가 깨어나며 화면을 납작하게 만든다.

깨어날 때 들어온 화면을 `viewBefore`에 적어두고, 죽을 때 그리로 돌려놓는다.

### 시체가 남는다

`despawnDelay`가 **0이면 "즉시 사라짐"이 아니라 "사라지지 않음"**이다. 죽은 자리에서 연출이 이어지는 무대가 있고, 반대로 즉시 사라져야 하는 보스는 없다. 쓰이지 않을 뜻을 0에 배정할 이유가 없다.

### Boss_Rise — 쓰러지는 클립을 거꾸로 훑는다

일으켜 세우는 데 새 클립을 만들지 않았다. 쓰러지는 클립을 끝에서 처음으로 훑으면 그것이 곧 일어나는 동작이고, 컨트롤러에 상태를 더할 일도 없다.

**배속을 음수로 두지 않는다.** 배속은 애니메이터 전체의 것이라 다른 레이어까지 거꾸로 돌고 어디서 멈출지를 이쪽이 알 수 없다. `Play(clip, 0, 1 - t)`로 프레임을 직접 지정하면 시작도 끝도 여기서 정해진다.

들어오면 나가지 않는 **흡수 상태**다. 되살아나는 것이 아니라 일어서는 그림만 필요한 자리이므로 `isDead`도 체력도 명부도 건드리지 않는다. 짝이 되는 주인공 쪽은 `Character_Faint`(가사)이고, 연출은 `CharacterCue`로 부른다 — 씬을 넘어 `CharacterRoot`를 꽂을 수 없어서다.

### 걷는 그림은 실제 이동에 맞춘다

Mixamo·라이트 팩 FBX는 **루프가 꺼진 채로 들어온다.** 진입할 때 트리거를 한 번 걸고 매 프레임 움직이면, 0.7초짜리 걷기가 마지막 프레임에서 멎은 채로 보스만 미끄러진다. Walk/Retreat/Idle 세 클립의 `Loop Time`을 켰고, in-place 클립은 첫·끝 프레임이 맞지 않으므로 `Loop Pose`도 함께 켠다.

배속은 `MoveSpeed` 파라미터로 `Walk`/`Retreat`의 Speed Multiplier에 물려 있다. 나눌 값은 **클립에게 직접 묻는다**(`clip.averageSpeed`) — 인스펙터에 초를 적어두면 클립을 갈아끼울 때 같이 고쳐야 하고, 잊은 쪽은 발이 미끄러지는 것으로만 조용히 드러난다.

`PlayIfNot`은 최적화가 아니다. **트리거는 전이가 가져갈 때까지 남으므로**, 같은 것을 매 프레임 세우면 남은 하나가 나중에 소비되어 스킬 도중에 걷는 그림이 끼어든다.

### 염동력 (공중 보스)

`Boss_Telekinesis`가 창을 끌어모아 돌리고 던진다. 던져진 창은 `Boss_Throwable`이고, 플레이어가 쳐낸 창은 `Phase.Captured`로 들어가 보스 주위를 돈다.

**되찾으러 가는 창은 아무것도 때리지 않는다.** `Strike()`는 `_damage <= 0`이면 그 자리에서 돌아선다 — 0을 실어 보내는 것은 "0만큼 때린다"가 아니라 "때리러 가는 것이 아니다"라는 뜻인데, 그대로 두면 보스의 `onDamage`가 깨어나 체력은 그대로인 채 보스만 휘청인다.

---

## 적 AI — Agnostos <sub>담당: phlnxciws5</sub>

### 한 겹 내려둔 이유

`Agnostos_Sober`가 루트의 기본 상태이고, 잠들고·다가가고·때리고·물러나는 것이 전부 그 안에서 일어난다. 예전에는 이 상태들이 루트에 직접 달려 있었다.

**한 겹 내려두면 피격이 "이 머신 전체를 통째로 밀어내는 것"이 된다.** 같은 층에 두면 *싸우는 중*과 *맞아서 끊긴 중*이 서로를 가로막는지 아닌지가 배치에서 읽히지 않는다.

피격 구독을 `Sober`에서 하는 것도 같은 이유다. 맞아서 이 머신을 떠나면 구독이 함께 풀리므로 **경직 중에 또 맞아 경직이 무한히 갱신되는 일이 구조적으로 생기지 않는다.**

### 무엇이 될지는 셀렉터가 고른다

`Agnostos_Damage`는 위에서부터 훑어 먼저 성립하는 것을 고른다 — 체력이 남았으면 경직, 다 깎였으면 사망. 조건이 늘면 목록에 한 줄이 늘 뿐이고 부르는 쪽은 달라지지 않는다. `Sober`는 "맞았다"만 알리고 무엇이 될지 묻지 않는다.

**사망은 이 머신 소속이 아니다.** 고르기만 하고 루트로 넘긴다. 경직은 지나가는 구간이지만 사망은 돌아오지 않는 끝이라, 둘을 같은 층에 두면 "피격이 끝나면 원래대로"라는 이 머신의 약속이 사망에서만 거짓이 된다.

### 값은 전부 반경과 시간이다

`AgnostosManager`가 드는 것은 반경 셋(`awakeRadius` · `attackRadius` · `retreatRadius`)과 시간 넷(`attackReadyTime` · `attackTime` · `attackCooldown` · `stunRecoveryTime`)이다. 상태는 이 값만 읽는다 — 경로도 조준도 없다. `OnDrawGizmos`가 반경을 그리므로 씬 뷰에서 바로 맞춘다.

### 공격은 클립이 아니라 다리를 옮기는 것이다

상태가 `footTargets[i].Transit(...)`으로 발 목표를 옮긴다. `Agnostos_Approach`는 `walkTargets`로, `Agnostos_Attack`은 `AttackReadyTargets` → `AttackTarget`으로 옮기고 도착 콜백에서 타격을 연다.

**타이밍이 인스펙터 값과 같은 자리에 있다.** 클립으로 만들면 `attackReadyTime`을 고칠 때마다 클립을 다시 잘라야 하고, 그 둘이 어긋나는 순간 판정이 그림보다 먼저 열린다.

`Agnostos`는 `DamagableBase`를 물려받는다 — 보스와 공유하는 자리이므로 잡몹 전용 로직을 그쪽에 넣지 않는다.

---

## 카메라 <sub>담당: phlnxciws5</sub>

`HybridCameraRig`는 **꽂아준 것을 따라간다** — 그것이 무엇인지 묻지 않는다. 플레이어를 찾아내는 일만 `PlayerCameraRig`가 물려받아 한 줄 더한다. 컷신의 주인공이나 탈것처럼 플레이어가 아닌 것을 따라가는 리그가 있을 수 있어서다. **수식을 복사해 오지 않는다** — 2D 오프셋도 전환 블렌딩도 3D 궤도도 두 벌이 되면 한쪽만 고치는 날이 온다.

배경 씬에 놓인 리그는 플레이어를 인스펙터로 꽂을 수 없다(씬을 넘는 참조). 그래서 `PlayerCameraRig`가 **매 프레임 비어 있는지 확인해** 채운다 — `Start` 한 번이 아닌 것은 씬이 올라오는 순서를 가정하지 않기 위해서다. 배경이 먼저 올라오면 그 순간에는 플레이어가 아직 없다.

### 상하각 한계

3D의 상하각은 `pitchMin3D`/`pitchMax3D`로 가둔다. **회전을 만들어놓고 오일러로 되돌려 자르지 않는다** — 쿼터니언에서 뽑은 오일러는 같은 회전을 여러 조합으로 표현할 수 있어, 위를 많이 볼 때 X가 아니라 Y·Z가 뒤집히는 쪽으로 나오고 그 값을 자르면 화면이 옆으로 돌아간다. 방향 벡터에서 수평각·상하각을 직접 재면 그런 갈림이 없다. ±89로 벌려두면 `LookRotation`과 같은 결과라, 쓰지 않는 동안의 동작은 그대로다.

### 자동 조준 의존

3D의 회전은 자동 조준(`AutoTarget`)이 고른 대상에서 나오고, 3D를 쓰는 무대에서는 그것이 비지 않아야 한다. 다만 **비었다고 멎지는 않는다** — 들고 있던 각도를 그대로 쓴다. 예전에는 `LateUpdate`가 타겟터를 요구해 자동 조준이 없으면 카메라가 통째로 멎었는데, 그건 없어도 되는 의존이었다.

### 벽시계로 감쇠한다

카메라 뒤쫓음은 `Time.unscaledDeltaTime`으로 돈다. 이유는 아래 [커튼·카메라는 벽시계로 돈다](#커튼카메라는-벽시계로-돈다)와 같다.

### CameraTakeover — 연출이 화면을 가져갈 때

부르면 지정한 `CinemachineCamera`에게 화면을 넘긴다. **넘기는 방법은 우선순위지 껐다 켜는 것이 아니다** — 끄면 브레인이 블렌딩할 상대를 잃어 화면이 뚝 끊긴다. 우선순위는 저작해둔 블렌드를 그대로 타므로 넘어가는 모습과 돌아오는 모습이 의도대로 나온다.

두 번 부르면 두 번째가 **이미 올려둔 값**을 원래 값으로 적어 돌려줄 곳을 잃는다. `_held`가 막는다. `OnDisable`에서 스스로 돌려주는 것도 같은 이유다 — 올려둔 우선순위가 남으면 그 카메라가 영영 화면을 쥔다.

씬을 넘는 참조는 담기지 않으므로 **대상은 같은 씬에 있어야 한다.**

---

## 그래픽 <sub>담당: phlnxciws5</sub>

### 셀 셰이딩 계보

| 자산 | |
|---|---|
| `Cel_rev1.shadergraph` | 1세대. 외부 애셋 절반이 아직 여기 물려 있다 |
| `Cel_rev2.shadergraph` | **현재 표준.** 기준 머티리얼은 `Ochyra.mat` |
| `SubGraphs/rev2`, `rev3` | rev3은 쓰기보다 먼저 만들어 두었다 |
| `Cel_rev2_ViewVortex_Tessellation.shader` | 손으로 쓴 테셀레이션 변종 (`CelTessellation.hlsl`, `VortexDeform.hlsl`) |

새 머티리얼은 `Ochyra.mat`에서 따온다. `Bricks_Plain`에서 씨앗을 뜨지 않는다.

### 렌더 피처 · 볼륨

`Scripts/PostProcessing/` — 외곽선, 볼류메트릭 라이팅/포그, 시야 마스크. 각각 `RenderFeature` + `VolumeComponent` 짝이다.

`FX/SlashDistortion.shader` — 베기 궤적. 화면 왜곡으로 그린다.

### 상태를 머티리얼로 옮기는 것

`BossWarpDriver` — 공중 보스의 체력과 시전 여부를 소용돌이 세기(`_T`)로 옮긴다. 체력이 닳을수록 세계가 일그러지고, 공격을 준비하는 동안 한 번 더 밀린다.

**부드럽게 만드는 일은 여기서 하지 않는다.** 목표값만 `MaterialFloatTransition`에 넘기고 그쪽이 제 시간과 곡선으로 따라간다. 두 곳에서 각자 다듬으면 어느 쪽 속도가 화면에 보이는지 알 수 없게 된다.

**값이 바뀔 때만 넘긴다.** 매 프레임 넘기면 그쪽 전환이 매번 처음부터 다시 시작해 첫 프레임을 영영 벗어나지 못한다 — 부드럽게 하려던 것이 오히려 멎는다.

### 외부 애셋

`Sun_Temple`은 **빌트인 렌더 파이프라인**으로 들어왔다. 마젠타로 보이는 것은 고장이 아니라 파이프라인이 다른 것이고, 절반은 `Cel_rev1`로 옮겨 두었다. `Shaders/SunTemple_URP/`가 URP 이식본이다.

---

## 씬 매니지먼트 <sub>담당: phlnxciws5</sub>

### 문 하나

씬을 옮기는 결정은 전부 `SceneDirector.Go(scene, place, facing, rest, mode)` 하나를 거친다. 관문(`Gateway`)도 부활(`Character_Down` → `SceneDirector.Respawn`)도 이 문으로 들어온다 — 씬을 넘는지, 배경만 가는지 세션을 나가는지는 **부르는 쪽이 알 일이 아니라 여기서 갈린다.**

```
Go(scene, place, facing, rest=false, mode=Additive)
  ├ 이미 로드됨          → 자리만 옮김 (Place + Restore2D)
  ├ Additive, 안 로드됨  → Transit() 코루틴 (커튼 → 언로드 → 로드 → 배치)
  └ Single               → SceneManager.LoadScene(scene, Single) 한 줄
                            (Character 씬까지 함께 내려간다 — 스테이지 이동이 아니라 세션을 나가는 것이다)
```

### Stage — 배경마다 스스로 등록

배경 씬 루트에 하나. `Awake`에서 정적 `Stage.Current`에 자신을 등록하고 `Start`에서 액티브 씬을 가져간다 — 하늘·환경광은 **액티브 씬** 것만 적용되는데, 배경은 늘 나중에 얹혀 스스로는 절대 액티브가 될 수 없기 때문이다. **Awake가 아니라 Start인 이유**: 비동기 로드는 씬이 "로드 완료"로 표시되기 전에 Awake를 돌리는데, `SetActiveScene`은 그 창에서 조용히 거부한다.

`_startIn2D`도 여기 있다. 스테이지에 들어올 때 2D로 시작할지 3D로 시작할지는 그 씬 자신이 가장 잘 아는 값이다(보스전은 3D가 흔하다).

**배경 씬에는 반드시 Stage가 하나 있어야 한다.** 없으면 `SceneDirector`가 무엇을 내려야 할지, 무슨 모드로 시작할지 몰라 경고만 남기고 이전 값을 그대로 쓴다.

### 전환 순서 — 왜 겹쳐 올리지 않는가

`Transit()`은 옛 배경을 완전히 내린 뒤에야 새 배경을 올린다. 겹쳐 올리는 방법도 있었지만 배경 두 벌의 텍스처가 동시에 상주하고, 새 스폰 자리에 옛 지오메트리가 아직 남아 물리가 몸을 밀어낸다. 캐릭터는 이 사이(발판이 없는 구간) 동안 **중력을 끄고 Pin**해 붙들려 있으므로 겹칠 이유가 없다.

`Destroy()`가 프레임 끝까지 미뤄지는 것도 문제였다 — 파괴 요청만으로는 그 프레임 안에서 콜라이더가 안 사라져 겹침이 남는다. 그래서 `SetActive(false)`로 먼저 물리에서 뺀 뒤 `Destroy()`한다.

로드 자체는 **동기**다(`SceneManager.LoadScene`, Async 아님). 조작은 이미 잠겨 있고 커튼이 화면을 덮은 뒤라, 프레임이 잠깐 멎는 대가로 "발판 없는 세상이 노출되는" 문제를 원천적으로 없앴다.

### 커튼·카메라는 벽시계로 돈다

이 게임은 체력에 시간 배속이 묶여 있다(`TimeManager`). 죽어 쓰러진 자리나 회복 중에는 배속이 거의 0까지 눌리는데, `TransitionCurtain`의 페이드와 `HybridCameraRig`의 카메라 뒤쫓음(`SmoothDamp`)이 `Time.deltaTime`을 쓰면 그 연출도 같이 멎는다. 캐릭터는 `Pin()`으로 이미 순간이동해 있는데 카메라만 안 따라가, 마치 "일어나는 키를 눌러야 카메라가 움직이는" 것처럼 보였다. 커튼·카메라 감쇠 둘 다 `Time.unscaledDeltaTime`으로 돌려 배속과 분리했다.

카메라를 순간이동시키지(Snap) 않는다 — 천천히 가로질러 따라가는 것이 의도다. 벽시계로 돌리는 것만으로 "안 움직이는 것처럼 보이는" 문제가 없어진다.

### 체크포인트 — 위치는 체크포인트가 정한다

예전에는 "말을 건 순간 캐릭터가 서 있던 자리"를 적었다. 지금은 **체크포인트 자신의 위치 + 인스펙터 오프셋**(`transform.TransformPoint(_offset)`)이다 — 말을 건 각도·위치에 따라 스폰 지점이 흔들리지 않고, 저작자가 씬 뷰 기즈모를 보며 바닥에 맞출 수 있다. 방향도 방향 벡터 하나로 고정한다(그때 플레이어가 보던 방향을 쓰지 않는다).

`SceneDirector`는 씬 이름·위치·회전·`riseOffset`(누워 있는 동안 메시를 몸에서 떼어놓는 거리 — 자리마다 바닥이 달라 체크포인트가 함께 넘긴다)만 받아 적을 뿐, 그 값을 어떻게 구했는지는 모른다. 등록하는 순간 최대 체력으로 회복된다 — `Heal(MaxHealth)`이지 `Revive()`가 아니다. 그쪽은 `Invulnerable`도 함께 풀어버려서, 살아있는 채로 찍는 체크포인트와는 뜻이 다르다.

### 붙들고 있어야 발동 — Input System의 Hold를 쓰지 않은 이유

체크포인트는 일정 시간 눌러야 발동한다. Unity Input System에 Hold 인터랙션이 있지만, 그건 `move_interact` **액션 하나**에 거는 것이라 게임의 모든 상호작용(문, NPC, 대사)이 다 같이 눌러야 하게 되고 시간도 전부 같은 값을 쓴다. 그래서 `InteractHold`가 따로 시계를 잰다 — `InputManager.CharacterInteract`(액션의 performed/canceled가 세우는 정적 눌림 플래그)를 매 프레임 읽어, 지금 조준된 대상이 자기일 때만(`InteractionDetector.Current`와 참조 비교) 채운다. 대상마다 시간과 Fill 이미지를 다르게 줄 수 있다.

`Checkpoint.Interact()`는 비어 있다 — FSM의 단발 입력(`Character_Interact`)이 여전히 이걸 부르지만 아무 일도 하지 않는다. 실제 발동은 `Activate()`이고, `InteractHold`가 다 채웠을 때만 부른다.

### 관문의 대사 — 순서, 그리고 두 갈래로 갈리는 이유

`Gateway`에 `Book`이 있으면 **대사가 다 끝나야** 전환이 시작된다(동시에 돌지 않는다 — 커튼과 겹쳐 나오거나 대사 중에 씬이 로드되는 문제가 있었다). `FlipBook.AddDialogueEndListener`로 구독해두고, 대사가 끝나야 `Cross()`를 부른다.

`ControlLock.Release()`(대사가 끝나면 조작을 돌려주는 곳)는 `SceneDirector.Busy`를 본다. 대사가 전환 도중(예: 사망으로 다른 경로가 끼어든 경우)에 자연히 끝나버리면, 아직 배경이 파괴되는 중인데 조작을 돌려줘 캐릭터가 움직이게 된다 — 그 창을 막는다.

관문이 대사 도중 스테이지와 함께 파괴되면(대사를 못 다 본 채로), `FlipBook.ForceClose`가 강제로 닫는다. `onDialogueNull`과 달리 **조작 잠금 해제는 여기 물리지 않는다** — 이미 다른 경로가 조작을 가져간 뒤일 수 있어서다.

### Additive vs Single — 스테이지 이동과 세션 종료는 다른 사건이다

관문은 두 목적지를 구분한다. **Additive**는 보통의 스테이지 이동(배경만 갈아끼움). **Single**은 타이틀로 돌아가는 것처럼 **게임 세션 자체를 나가는 것**이다 — Character 씬까지 포함해 지금 올라온 모든 씬을 내린다. `SceneDirector.Go()`가 Single이면 `Transit()` 코루틴을 거치지 않고 `SceneManager.LoadScene(scene, Single)` 한 줄로 끝낸다 — 자리를 계산하거나 캐릭터를 붙들 이유가 없다. 그 캐릭터 자체가 이 전환과 함께 사라지기 때문이다.

### 타이틀 → 게임 (AdditiveSceneLoader)

`Assets/Antigravity/AdditiveSceneLoader.cs`(다른 손이 만든 것)가 커튼을 덮고 씬 목록(Character → UI-General → 첫 배경)을 순서대로 불러온다. 오프닝 대사(`openingScript`)를 커튼보다 먼저 재생하도록 얹었는데, **대사가 재생될 시점엔 `UI - General`(FlipBook이 있는 곳)이 아직 로드 전이다** — 로드 목록 안에 있으니까. 그래서 Title 씬이 `TransitionCurtain`을 자체 복제해 쓰는 것과 같은 이유로, **Title도 FlipBook을 자체적으로 하나 갖는다**(UI-General 것의 최소 축약판).

### StageDirectory — 로딩 화면이 이름을 미리 알아야 한다

전환 커튼에 "다음 지역: OOO"를 띄우려면 **씬이 로드되기 전에** 그 이름이 있어야 하는데, 씬 자신에게 물어볼 방법이 없다(아직 안 올라왔으므로). `StageDirectory`는 씬 이름 → 표시 이름 사전을 별도 에셋으로 들고, `SceneDirector.RegionName()`이 여기서 찾는다.

---

## 레벨 디자인 · `AutoPlatform` <sub>담당: phlnxciws5</sub>

발판을 손으로 놓는 대신 **동선을 먼저 적고 발판을 거기서 뽑는다.** 노드 하나가 "여기 서 있을 수 있다"는 뜻이고 노드 사이가 한 번의 이동이다. 튜닝으로 점프가 바뀌었을 때 무엇이 깨졌는지 **눈이 아니라 판정으로** 알기 위해서다.

### 이동 성능을 여기에 적어두지 않는다

`MotionProfile`은 값을 자기가 들지 않고 `ReadFrom`으로 `CharacterManager`에서 그대로 긁어온다. **튜닝이 한 곳에서만 일어나야 배치와 실제 움직임이 어긋나지 않는다.** 인스펙터의 필드는 캐릭터가 아직 씬에 없을 때 쓰는 기본값이자, 지금 무엇으로 계산 중인지 보여주는 창이다.

### 공중 자원은 티켓이다

`Traversal` — 한 번의 공중 이동에서 쓰는 자원이고 착지하면 전부 되돌아온다. 지상 점프는 티켓을 쓰지 않는다. `Character_Ground`가 속도만 싣는다.

**되돌아 점프를 따로 둔 이유**는 그것으로만 닿는 자리가 있기 때문이다. 수평 속도가 공중에서도 즉시 뒤집히므로 나갔다가 되돌아올 수 있고, 그래야 제자리 바로 위나 뒤쪽 위의 발판에 오른다 — 방향을 고정한 채로는 절대 닿지 못하는 자리다. 나를 덮고 있는 더 큰 발판 위로 오르는 것도 이것이다.

### 사람이 정하는 것은 넷뿐이다

`RouteFromBounds` — 어디서 어디로, 어느 부피 안에서, 몇 번 접어서. 걸음의 높이와 거리는 도달 구간에서 역산하고, 놓기 전에 빈자리인지 확인한다. **그래야 만들어진 자리가 반드시 밟히고 서로를 막지 않는다.**

구간은 성격이 다른 갈래를 씨앗으로 섞는다. 지그재그만 반복하면 수직성은 얻어도 지루하다.

씨앗을 훑는 동안에는 로그를 닫는다. 버려질 후보가 뱉는 경고까지 다 찍으면 정작 채택된 결과의 경고가 그 속에 묻힌다.

### 늘리지 않고 타일링한다

`PlatformBuilder` — 폭은 **모듈 개수로만** 만든다. Deck 계열 장식은 Prefab Filler가 로컬 격자로 채워 넣은 `_Fill_*` 컨테이너라, 루트를 X로 늘리면 석조 장식이 함께 늘어나 뭉개진다.

모듈 크기는 하드코딩하지 않고 프리팹의 콜라이더에서 잰다. **아트가 바뀌면 결과도 따라 바뀐다.**

아트의 세로 부피를 콜라이더와 따로 들고 있다. 이 애셋들은 걸어 다니는 슬래브가 아니라 아래로 열주가 매달린 신전 조각이라, 딛는 면만 보고 배치하면 서로 관통한다. 반대로 통행 여유를 아트로 재면 **있지도 않은 벽을 요구하게 된다** — 종유석처럼 콜라이더가 없는 장식은 지나가는 데 지장이 없다.

### 버튼 하나로 묶은 이유

`LevelBuilder` — 치수 재기 · 이동 성능 읽기 · 씨앗 고르기 · 동선 만들기 · 발판 짓기 · 검사. **순서를 사람이 기억해야 하면 반드시 어느 하나를 빠뜨리고, 빠뜨린 것이 무엇인지는 결과가 이상해진 뒤에야 알게 된다.**

진행 창은 사람이 버튼을 눌렀을 때만 띄운다. 스크립트로 돌릴 때 모달을 띄우면 그쪽이 멈춘다.

> **재생성은 파괴적이다.** 손으로 다듬은 것을 지우고도 "통과"라고 보고한다. 다시 돌리기 전에 물어본다.

---

## UI · 대사 흐름 <sub>담당: phlnxciws5</sub>

이 절의 결정 대부분이 한 가지 제약에서 나온다. **UI는 다른 씬에 있고, 씬을 넘는 참조는 유니티가 저장하지 못한다.** 인스펙터에 꽂아둔 것처럼 보여도 로드되면 비어 있다.

### 바깥이 UI에 말을 거는 창구는 하나다

`HudService` — 자기를 등록해 두고 바깥은 `Instance`로 런타임에 찾는다. `FlipBook` · `SceneDirector`와 같은 방식이다.

**안쪽 구성을 밖에 알리지 않는다.** HUD를 올리는 데 페이더가 몇 개 필요한지는 여기서만 안다. 묶음이 늘거나 줄어도 고칠 곳은 이 목록 하나다.

### 대사는 책을 넘기는 일이다

`FlipBook`이 책의 유일한 주인이고, `MessageUI`가 화면 중앙 문구의 유일한 주인이다. 표시 여부를 전부 페이더에 넘기고 `SetActive`를 직접 부르지 않는다 — **알파와 활성 상태가 어긋날 자리를 없애기 위해서다.**

예전에는 셋이 각자 활성 상태를 건드렸다. 그래서 대사를 한 번 열면 페이더가 오브젝트를 되살리고, 같이 얹혀 있던 `Instructor`가 옛 키 배선을 든 채로 부활했다.

**책을 거두고 나서 알린다.** 순서가 뒤바뀌면, 그 알림을 듣고 새 책을 연 쪽의 책을 뒤이은 `RemoveBook`이 지운다 — 화면에는 대사가 떠 있는데 안은 비어, `Next`가 첫 줄에서 돌아나가 영영 안 넘어간다.

넘길 책이 없으면 조용히 지나간다. Enter가 상시 물려 있어 책을 거둔 직후에도 그 프레임에 남은 입력이 새어 들어온다.

### 여는 입구는 하나

`BookHolder.Open` — 밟아서 열려면 `CollisionInvoke`의 `onCollision`에, 말을 걸어 열려면 `InteractableEvent`의 `onInteract`에 물린다. **발동 방식과 무관하다.** `Gateway`가 `Enter()` 하나로 두 길을 받는 것과 같은 모양이다.

### 조작을 가져가고 돌려주는 자리

`ControlLock` — `FlipBook`의 `onSetBook` → `Lock`, `onDialogueNull` → `Release`.

`CharacterRoot`를 인스펙터로 꽂을 수 없어서 있는 컴포넌트다. **루트를 직접 들지 않는 이유이기도 하다** — 캐릭터가 갈려도 `PlayerManager`가 가리키는 곳만 바뀌면 된다.

### 보스 체력바는 서로를 모른다

`BossHealthVM`은 스크립터블 오브젝트다. **씬이 아니라 파일이기 때문이다** — 씬을 넘는 참조는 저장되지 않지만 애셋 참조는 어디서든 저장된다. 지금은 보스와 바가 같은 스테이지 씬에 있어 그 문제가 없지만, 나중에 상시 UI로 옮기더라도 배선만 바뀌고 코드는 그대로다.

스테이지마다 하나씩 둔다. 그래야 두 보스가 같이 나와도 서로의 값을 덮지 않는다.

**런타임 값은 저장하지 않는다.** 애셋은 판이 끝나도 파일로 남으므로, 저장했다가는 죽기 직전의 체력이 파일에 적혀 다음 판이 거기서 시작한다.

`BossHealthPresenter`는 보스 프리팹에 붙고 **UI를 모른다.** 바가 있든 없든, UI 씬이 아직 안 올라왔든 하는 일이 같다. 띄우고 내리는 시점은 이미 짝이 맞은 자리를 쓴다 — `Boss_Idle.ToAlert()`(화면이 3D로 열리는 지점)와 `Boss_Dead.Enter()`(2D로 되돌리는 지점).

---

## 빌드 · 배포 <sub>담당: phlnxciws5</sub>

### 기한이 지나면 스스로를 지운다

졸업작품 설정에서 게임 세계의 붕괴가 현실의 빌드 삭제로 이어진다. `BuildExpiry`가 그것이다. **이 기능을 켜고 빌드한 것이 아니면 아무 일도 일어나지 않는다.**

**바깥에 두는 검사기가 아니라 빌드 안에 심는다.** 실행 파일 옆에 배치 파일을 동봉하는 방식은 그 배치를 눌렀을 때만 걸린다 — exe를 직접 실행하면 아무 일도 없고, 파일 하나만 지우면 기한도 함께 사라진다. 여기서는 게임이 켜지는 길목(`RuntimeInitializeLoadType.BeforeSplashScreen`)을 잡으므로 우회할 자리가 없다.

**관리자 권한을 쓰지 않는다.** 예약 작업도 서비스도 레지스트리도 건드리지 않는다. 하는 일은 제 폴더를 지우는 것뿐이고, 그건 그 폴더를 만든 사용자면 할 수 있다. 대신 **Program Files 밑에 두면 쓰기 권한이 없어 삭제가 조용히 실패한다** — 사용자 폴더 쪽에 두고 배포한다.

**시계를 되돌려도 소용없다.** 실행할 때마다 지금까지 본 가장 나중 시각을 남기고, 다음에 켤 때 그 시각과 현재 시각 중 늦은 쪽을 쓴다. 흔적은 빌드 폴더가 아니라 `Application.persistentDataPath`에 두어 폴더째 옮기거나 복사해도 따라온다.

**지우는 일은 자기가 못 한다.** 윈도우가 실행 중인 exe를 잠그기 때문이다. 임시 폴더에 작은 청소 스크립트를 떨어뜨리고, 그것이 이 프로세스가 완전히 내려가기를 기다렸다가 폴더를 지운 뒤 자기 자신까지 지운다.

**날짜는 코드에 박는다.** 옆에 설정 파일로 두면 그 파일이 곧 해제 키가 된다.

### 심사용 런처 — `DemoTimebomb/`

유니티 빌드를 `Game/` 하위로 넣고 런처를 최상단에 두는 배포 형태. 기한 전에는 게임을 대신 실행하고, 지난 뒤에는 데모 폴더의 내용물을 비운다. 날짜는 `src/Program.cs` 상단의 `Expiry` 하나다.

삭제 대상 경로는 배치 본문에 적지 않고 환경변수로 넘긴다 — **cmd가 배치를 시스템 코드페이지로 읽어 한글 경로가 깨지기 때문이다.** 배치 본문은 순수 ASCII로 둔다.

드라이브 루트 · 사용자 폴더 · 바탕화면 · 문서 · Program Files, 그리고 그것들의 상위 경로는 안전장치가 거른다. 그래서 압축을 풀 때 **자체 폴더 안에** 두어야 동작한다.

로컬 시한장치의 한계는 그대로다 — 시계 되돌리기, 미리 복사해 두기, 런처를 지우고 `Game/`을 직접 실행하기. **정직한 심사자를 대상으로 한 자동 정리이지 복제 방지가 아니다.**

---

## 이펙트 · 연출 · 발판 <sub>담당: Claude Opus 5</sub>

### 베기는 색이 아니라 왜곡이다

`FX/SlashDistortion.shader` — 호를 칠하지 않고 **뒤에 있던 화면을 밀어낸다.** 색으로 그리면 밝은 신전에서는 흰 호가, 어두운 굴에서는 검은 호가 배경에 묻힌다. 미는 것은 배경이 무엇이든 읽힌다.

반원 메시의 UV가 U는 호를 따라가는 각도, V는 날의 폭으로 깔려 있어 **텍스처 없이 마스크가 나온다.** 미는 방향도 UV의 화면 미분에서 뽑으므로 메시를 어느 각도로 눕혀도 따라온다 — 방향을 상수로 적어두면 한쪽으로 벨 때만 맞는다.

둘 중 하나가 빠지면 이펙트가 통째로 안 보인다. URP 애셋의 **Opaque Texture**(`PC_RPAsset`은 켜져 있다), 그리고 파티클 렌더러의 **Custom Vertex Streams에 UV 다음으로 꽂은 AgePercent**다. 뒤엣것이 없으면 순차로 드러나는 값이 0에 고정된다. 안 보이면 여기부터 본다.

### 이펙트를 세우는 것은 때린 쪽이다

`WeaponEffectSpawner` — 부르면 그 자리에 이펙트를 하나 세운다. **부르는 시점을 여기서 되읽지 않는다.** 상태를 읽기 시작하면 스킬이 늘 때마다 조건이 하나씩 붙는다.

맞은 쪽에서 부르지 않는 이유는 **그쪽이 무엇에 맞았는지 모르기 때문이다.** `IDamageable.TakeDamage`는 숫자 하나만 받으므로 방향도 무기도 알 수 없고, `KillVolume`도 같은 길로 들어온다 — 맞은 쪽에서 띄우면 구덩이에 떨어져 죽는 것이 칼에 베인 것으로 보인다. 때린 쪽에서 부르면 `MeleeWeapon`이 한 스윙에 같은 적을 한 번만 통과시키는 것도 그대로 얻는다.

`MeleeWeapon`은 **건드리지 않았다.** 주인공(`PlayerManager.playerWeapon`)·보스(`BossManager.weapon`)·Agnostos(`AgnostosManager.weapon`)가 함께 쓰는 판정 코드라, 연출 하나 붙이자고 손대면 셋이 같이 흔들린다. 그래서 이 부품은 판정에 끼어들지 않고 **불러주기를 기다리기만 한다.**

`PlayAt`이 자리와 각도를 따로 받는 것은 **베기 궤적이 칼이 있는 곳에 서지 않기 때문이다.** 궤적은 칼이 지나간 끝에 서야 하는데, 뿌리는 시점의 칼은 아직 시작점에 있다. 각도도 무기가 아니라 궤적이 정한다.

`PlayAt(Collider)`는 트리거 충돌에 접점이 없어서 — `OnTriggerEnter`가 넘기는 것은 상대 콜라이더뿐이다 — 가장 가까운 점으로 대신한다. 상대가 **볼록하지 않은 `MeshCollider`면 유니티가 조용히 중심점을 돌려주므로** 그런 적에게만 몸통 한가운데에 뜬다.

이름은 무기지만 무기 전용이 아니다. `CharacterManager.HealEffect`가 같은 부품으로 회복 연출을 세운다.

### 스포너가 뒷정리를 맡는다

**Cartoon FX 프리팹은 `stopAction`이 `None`이라 다 뿌리고도 빈 오브젝트로 남는다.** 보이는 것이 없으니 씬이 무거워질 때까지 아무도 눈치채지 못한다. 그래서 세운 쪽이 치운다.

치울 시각은 초로 적어두지 않고 **파티클에서 직접 잰다**(`duration + startLifetime` 중 가장 긴 것). 숫자를 적어두면 이펙트를 갈아끼울 때마다 같이 고쳐야 하고, 잊은 쪽은 꼬리가 잘리거나 빈 오브젝트가 오래 남는 것으로 조용히 드러난다. 파티클이 하나도 없는 이펙트 — 빛이나 데칼만 든 것 — 는 길이를 알 수 없어 2초로 떨어진다.

`Stop()`은 **방출만 멈춘다.** 이미 나온 알갱이는 제 수명대로 사라지고, 치우는 일은 세울 때 걸어둔 `Destroy`가 그대로 맡는다 — 여기서 또 지우면 같은 것을 두 곳에서 치우게 된다. 오래 끄는 회복 연출을 도중에 거두는 `Character_Heal`이 이것을 쓴다. **마지막에 세운 하나만 기억하므로** 겹쳐 뿌린 것까지 거두지는 못한다.

`EffectEnabled`를 꺼도 **이미 서 있는 것은 걷어가지 않는다.** 터지는 중인 이펙트가 도중에 사라지는 것은 꺼진 것이 아니라 고장 난 것으로 보인다.

이펙트는 **부모 없이 월드에 선다**(`followWeapon` 꺼짐, 파티클도 World 시뮬레이션). 벤 자리에 남아야 할 것이 칼을 따라다니면 휘두르는 내내 호가 끌려간다. 무기에 매달아야 하는 연출만 그 스위치를 켠다.

> `WeaponSwingEffect`는 판정 콜라이더가 열리는 것을 보고 스포너를 대신 불러주던 부품이다. 호출을 스킬 코드가 직접 하기로 하면서 자리를 잃어, **지금은 어느 프리팹도 씬도 참조하지 않는다.** 자동 발동으로 되돌릴 생각이 없으면 지워도 된다.

### 시야 마스크는 자기 모양이 없다

`VisionMask` — 카메라가 그리는 것을 통째로 덮고, `VisionRevealSource` 구(球)만 그것을 도로 연다. `ShadowVolume`처럼 제 부피로 가리는 방식이 아니다.

셰이딩 **뒤** 화면 공간에서 도므로 조명 항이 아니라 최종 색을 누른다. 자체발광도 함께 어두워진다 — 어둠 속에서 빛나야 하는 것은 리빌 구 안에 들어와 있어야 한다.

한 번에 하나만 그린다. 배경 씬을 겹쳐 올리는 구성이라 둘 이상 올라오는 일이 실제로 생기고, 그때마다 곱해지면 스테이지를 옮기는 것만으로 화면이 점점 검어진다. 여럿이면 `priority`가 가장 높은 하나가 이긴다.

### 외곽선의 광원 반영은 본체와 따로 잡는다

`_OutlineLightInfluence` (`Cel_rev2_*_Tessellation.shader`) — 0이면 `_OutlineColor` 그대로, 1이면 본체와 같은 정도로 빛을 받는다. **기본값은 0이라 켜기 전에는 예전과 같다.**

본체와 외곽선이 **같은 `LightTint()`를 부른다.** 갈리는 것은 세기 인자 하나다. 프래그먼트마다 인라인으로 두면 조명 모델을 손볼 때 본체만 고치고 외곽선은 옛 식으로 남아 조용히 갈라진다.

외곽선은 **밀려난 자리**에서 빛을 샘플한다. 선이 실제로 그려지는 자리가 거기다.

대가는 변형 수다. 외곽선 패스가 빛을 읽으려면 본체와 같은 조명 키워드가 있어야 해서 `multi_compile` 다섯 줄이 붙었다. 값이 0이면 `LightTint`가 즉시 빠져나가 런타임 비용은 없지만 컴파일 시간은 늘어난다. 안 쓰기로 하면 그 다섯 줄을 지우면 된다.

### 연출을 인스펙터에서 엮는 세 부품

`RaiseInvoke` · `TimedInvoke` · `UiVisibility` — **셋 다 무엇이 듣는지 모른다.** 발동 조건만 다르고(일어나기 키 / 시간 / 밖에서의 호출) 무엇을 할지는 `UnityEvent`가 정한다. 연출 하나마다 스크립트를 새로 쓰지 않으려는 것이다.

`TimedInvoke`의 기본은 **벽시계**다. 이 게임은 체력이 `TimeManager`로 시간 배속을 끌어내리므로, 스케일 시계로 재면 빈사 상태에서 3초가 10초가 되고 일시정지 중에는 영영 끝나지 않는다. 코루틴이 아니라 `Update`로 세는 것은, 껐다 켜는 동안 타이머가 죽지 않고 멈춰 있다 이어지게 하기 위해서다.

`RaiseInvoke`는 키보드를 직접 읽지 않고 `InputManager.move_raise` 액션을 듣는다. 리바인딩과 게임패드가 그대로 따라오고, 키가 무엇인지 아는 곳이 `InputManager` 하나로 남는다.

`UiVisibility`는 **무엇을 접는지 모른다.** UI가 페이더 몇 개로 되어 있는지는 `HudService`만 알고 이쪽은 "접어라 / 펴라"만 말한다. 묶음이 늘어도 이 파일은 그대로다.

### 무너지는 발판

`Platforms/Crumble` — 닿는 즉시가 아니라 **떠나는 순간** 사라진다. 밟자마자 없애면 딛고 뛸 것이 없어져 그냥 구덩이가 된다.

`OnCollisionExit`를 쓰지 않는다. 그 신호는 발판 위를 걷는 도중에도 한두 스텝씩 튀어나와, 그대로 쓰면 서 있기만 해도 바닥이 빠진다. `OnCollisionStay`로 **마지막으로 닿아 있던 시각**을 적어두고 그것이 충분히 오래됐을 때 떠난 것으로 본다. 유예는 기본 0.1초 — 캐릭터의 코요테 타임과 같은 값이다. 이보다 짧게 두면 **이미 사라진 발판에서 코요테 점프하는 순간**이 생긴다.

플레이어의 트리거 콜라이더는 거른다. 몸에는 공격 판정(`HitSweep`)도 달려 있고 그것도 `PlayerManager.IsPlayer`에는 플레이어로 잡힌다. 걸러내지 않으면 발판 옆에서 칼을 한 번 휘두른 것이 밟은 것이 된다.

오브젝트를 끄지 않고 콜라이더·렌더러만 끈다. 끄면 이 컴포넌트도 함께 멎어 돌아올 시간을 셀 것이 없어지고, 밖에서 `Restore()`를 부르는 길도 막힌다.

> **부활은 이것을 되돌려주지 않는다.** 같은 스테이지에서 죽으면 씬을 다시 올리지 않고 자리만 옮기므로 무너진 발판은 무너진 채로 남는다. 그 발판이 유일한 길이면 죽는 순간 진행이 막힌다. `Return`에 시간을 주거나, 체크포인트 쪽에서 `Restore()`를 부를 것.

### 턱은 떨어지는 중에만 잡힌다

레벨 판정이 쓰는 천장(`MotionProfile.LedgeCeiling`)은 `reachHigh`가 아니라 **`reachLow`** 로 잰다.

`LedgeGrab.Probe`는 발끝 + `reachHigh`에서 발끝 + `reachLow`까지 아래로 훑고, `Character_Airborne.ToLedge`는 `if (Rising) return;`으로 **떨어지는 중에만** 건다. 그래서 이론상 한계인 정점 + `reachHigh` 턱은 정점에 닿은 그 프레임에 이미 하강 중이어야만 잡히는, 사람이 맞출 수 없는 자리다. 하강하며 창을 온전히 지나는 높이는 정점 + `reachLow`고 그것이 설계에 쓸 수 있는 한계다. 4.40 m → **4.05 m**.

0.35 m를 잃자 동굴 세트가 층을 못 접었는데, 원인은 따로 있었다. "촘촘히 쌓이는 발판"을 **아트가 늘어진 길이로만** 뽑고 있었다. 종유석은 서로 안 닿기만 하면 되지만 콜라이더는 그 위로 사람이 지나갈 공간까지 요구하므로, 아트가 짧아도 콜라이더가 통짜인 발판이 더 두껍게 쌓인다. 둘 중 **큰 쪽**으로 재는 `LevelRoute.StackCost`로 바꾸니 4.29 m → 3.19 m가 되어 다시 접힌다.

---

## 에디터 툴 — 미사용 셰이더 정리 <sub>담당: Claude Opus 5</sub>

`Tools/Unused Shader Cleaner` (`Assets/Editor/UnusedShaderCleaner.cs`). 빌드 세팅에 등록된 씬에서 출발해 의존성을 훑고, 거기 걸리지 않은 `Assets/` 아래 셰이더를 목록으로 낸다. 지우는 것은 그다음이고, 목록에서 **고른 것만** 지운다.

### 목록은 결론이 아니라 후보다

뿌리는 **빌드 세팅 씬**이지 프로젝트 전체가 아니다. 그래서 작업 중인 씬이나 테스트 씬에서만 쓰는 셰이더는 정의상 미사용으로 잡힌다. 목록을 그대로 믿고 전부 지우라고 만든 것이 아니라, **눈으로 훑을 범위를 줄이려고** 만든 것이다.

체크가 꺼진 씬도 기본값으로는 뿌리에 넣는다. 빌드에서 빠져 있을 뿐 나중에 다시 켤 씬이고, 그 사이에 셰이더가 사라지면 켜는 순간 분홍색이 된다.

### 왜 남았는지를 같이 보여준다

씬 의존성만으로는 못 잡는 길이 다섯 갈래 있다. 각각을 보호 규칙으로 두고, 규칙에 걸려 살아남은 셰이더는 **"보호됨" 목록에 이유와 함께** 뜬다.

| 규칙 | 씬 의존성이 놓치는 이유 |
|---|---|
| Resources / StreamingAssets | 경로로 불러오므로 참조가 어디에도 안 적힌다 |
| 렌더 파이프라인 설정 | URP 애셋·렌더러 데이터·Always Included Shaders는 씬 밖에 있다 |
| Editor 폴더 | 빌드에 안 들어가는 것이 맞다. 지우면 에디터 툴이 깨진다 |
| 스크립트 문자열 | `Shader.Find("...")` — 이름만 적혀 있다 |
| 셰이더 → 셰이더 | 서브그래프, `Fallback`, `UsePass` |

이유를 안 보여주면 다음 사람이 규칙을 처음부터 다시 발견한다. 목록에서 조용히 빠지는 것과 "렌더 파이프라인이 쓰고 있어서 뺐다"고 적혀 있는 것은 다른 정보다.

이름이 변수로 들어가는 `Shader.Find`는 판정할 수 없다. 그런 호출이 몇 곳 있는지만 세어 경고로 띄운다 — **판정하지 못했다는 사실 자체가 정보이기 때문**이다.

### 셰이더가 셰이더를 부르는 자리는 텍스트로 훑는다

그래프가 서브그래프를 참조하는 것은 임포터가 등록하는 의존성이라, `GetDependencies`가 알려주리라고 **믿고 맡기지 않았다.** 살아남은 그래프 파일을 열어 후보의 GUID가 본문에 있으면 그것도 살린다. `.shader`의 `Fallback`/`UsePass`는 GUID가 아니라 셰이더 **이름**으로 적히므로 이름으로 한 번 더 대조한다.

살린 것에서 다시 뻗어 나가야 하므로 이 추적은 큐로 돈다. 서브그래프가 서브그래프를 부르는 깊이가 실제로 있다.

### 이름을 얻으려고 에셋을 로드하면 에디터가 멎는다

처음에는 셰이더 이름을 `AssetDatabase.LoadAssetAtPath<Shader>`로 얻었다. 후보 100여 개를 전부 로드하니 **셰이더 임포트가 딸려 들어와 에디터가 몇 분 동안 응답하지 않았다.** 실제로 한 번 그렇게 멎었다.

지금은 파일 텍스트만 읽는다 — `.shader`는 첫 줄의 `Shader "이름"`, 그래프는 `m_Path` + 파일 이름이 곧 셰이더 이름이다. 머티리얼 참조 수(`mat ×N`)도 같은 이유로 `.mat`을 로드하지 않고 YAML에서 셰이더 GUID만 뽑아 센다. 프로젝트가 `ForceText` 직렬화라 쓸 수 있는 방법이다.

### 지우는 방법

기본은 **휴지통으로 이동**(`MoveAssetsToTrash`)이라 되돌릴 수 있다. 완전 삭제는 토글을 꺼야 한다. 어느 쪽이든 `AssetDatabase`를 거친다 — 에디터를 켠 채 파일을 직접 지우면 `PC_Renderer.asset`에서 렌더러 피처가 조용히 사라지기 때문이다(→ *함께 지키는 것*). 플레이 모드에서는 삭제 버튼이 잠긴다.

스캔은 `UnusedShaderCleaner.Scan(options)` 정적 메서드로 빠져 있어 창 없이도 부를 수 있다.

### 한 번 걷어낸 것 (`8bc937b`)

셰이더 애셋 **34개, 약 5만 줄**. Stylized Water 데모, Shader Graph 샘플의 Custom Lighting 넷, TidalFlask 폴리지·삼면투영 변종, `Sun_Temple`의 빌트인 원본 전부와 쓰지 않게 된 URP 이식본 다섯(`Decal_URP` · `Foliage_URP` · `VertexBlend_URP` 셋이 남았다), 그리고 Cel 계보에서 갈라졌다가 쓰이지 않은 `Cel_phlnxciws5` · `Cel_rev2_Vortex` · `Invisible`.

> `Sun_Temple`의 빌트인 셰이더가 사라졌으므로, 아직 옮기지 않은 머티리얼은 이제 "파이프라인이 달라서 분홍"이 아니라 "셰이더가 없어서 분홍"이다. 화면은 같고, 고치는 길만 하나로 줄었다 — `Cel_rev2`나 `SunTemple_URP/`로 옮기는 것.

> **`Sun_Temple`은 밖에서 들여온 애셋이다.** 우리가 쓴 셰이더가 아니고, 남은 자산도 **혹시 몰라 일부러 남겨둔 것**이다. 미사용으로 잡히더라도 지우지 않는다. 스캔할 때마다 목록에 올라오는 것이 성가시면 제외 경로에 `Assets/Art/External/Sun_Temple/`을 넣는다.

---

## 에디터 툴 — 환경 배치 <sub>담당: Claude Opus 5</sub>

창 다섯과 공유 부품 하나. **프리팹 목록·가중치·스케일 범위·시드는 전부 `EnvironmentPlacerProfile` 하나를 나눠 쓴다** — 배치 방식이 달라도 "무엇을 심을까"는 같은 질문이라, 툴마다 자료 형식을 새로 만들면 같은 풀 목록을 다섯 번 관리하게 된다.

| 창 | 어디에 뿌리나 | 언제 쓰나 |
|---|---|---|
| `Tools/Environment Placer` | 대상 AABB의 여섯 면 | 축에 정렬된 블록 |
| `Tools/Surface Scatter` | 레이캐스트로 찾은 **실제 표면** | 기울어진 면, 굴곡진 지형, 덩굴 |
| `Tools/Prefab Filler` | 대상 **부피**의 균일 격자 | 속을 채울 때 |
| `Tools/Foreground Band` | 레벨 X를 가로지르는 띠 | 카메라 앞을 스쳐 지나가는 풀 |
| `Tools/Foreground Projector` | 원본을 카메라 기준으로 축소 복제 | 원본과 화면에서 겹치는 전경 |

`PoissonDisk.cs`는 Bridson 샘플러를 정적 클래스로 뺀 것이다. `EnvironmentPlacerWindow`는 같은 알고리즘을 아직 자기 안에 들고 있다 — **잘 돌고 있어서 건드리지 않았다.** 정리한다면 그쪽 호출을 이 클래스로 바꾸면 된다.

### 배치물은 루트에서 완성하고 마지막에 편입한다

네 툴이 공유하는 규약이고, 여기가 이 절에서 가장 값비싸게 배운 자리다.

컨테이너를 **대상의 자식으로 먼저 만들면** 대상의 비균일 스케일이 위에서 내려온다. 그 아래에서 비스듬히 회전한 배치물은 **전단(shear)** 으로 찌그러진다. 부모 스케일 `(3, 1, 5)`에 자식이 `Euler(35, 40, 12)`로 돌아 있을 때 실측:

```
축 길이  0.728 / 0.891 / 0.796      (0.700 세 개여야 정상)
축 각도  122.9 / 28.7 / 110.8       (90 세 개여야 정상)
lossyScale  (0.700, 0.700, 0.700)   ← 거짓말을 한다
```

**스케일은 축의 길이만 바꾸고 축 사이 각도는 못 바꾼다.** 그래서 나중에 보정할 방법이 없고, 인스펙터 숫자로는 보이지도 않는다.

지금은 컨테이너를 **씬 루트에서, 대상의 월드 회전에 맞춰** 만들고 다 채운 뒤 `SetParent(target, true)`로 통째로 넣는다. 루트에 있는 동안은 스케일이 항등이라 배치물이 영향을 받지 않고, 마지막 한 번의 편입에서 유니티가 `localScale`을 역산해 대상 스케일을 정확히 상쇄한다. 회전을 대상에 맞춰 두는 것이 그 상쇄가 성립하는 조건이다 — 항등으로 두면 대상과의 상대 회전이 남아 그것만으로 전단이 생긴다(실측 55/79/98).

한 경우는 여전히 못 고친다. **조상 체인이 이미 회전 + 비균일 스케일이면** 내려오는 행렬 자체가 찌그러져 있다(각도 54.82). 어떤 스케일을 곱해도 그 각도는 그대로다. 자식으로 남아 있는 한 방법이 없다.

> 예전 버전은 컨테이너에 `1 / lossyScale`을 걸어 상쇄했다. **`lossyScale`은 조상에 회전이 섞이면 근사값이라** 그 역수로는 상쇄가 성립하지 않는다. 한 축이 0이면 `1/0 = Infinity`가 나와 유니티가 대입 자체를 거부한다. → *함께 지키는 것*

배치 순서는 **회전 → 스케일 → 위치**로 고정했다. 루트에서는 셋이 서로 독립이라 순서를 바꿔도 결과가 같다는 것을 측정으로 확인했다(A/B/C 동일). 그래도 고정해 두는 것은, 나중에 부모 밑에서 값을 만지는 코드가 끼어들 때 안전한 순서이기 때문이다.

### 취소는 "아무 일도 없었음"이어야 한다

다섯 툴 모두 `Undo.IncrementCurrentGroup()`으로 그룹을 열고, 취소되면 `RevertAllDownToGroup`으로 그룹째 되돌린다. 컨테이너 생성도, 시작할 때 지운 기존 배치물도 전부 그 그룹 안에 있다.

절반만 깔린 결과를 남기지 않는 것이 핵심이다. 수천 개를 뿌리는 도중 멈추면 **어디까지 진행됐는지 사람이 알 수 없으므로**, 되돌릴 수 없는 취소는 취소가 아니다. 진행바는 Poisson 샘플링 루프에도 걸려 있다 — 오브젝트가 하나도 생기기 전에 에디터가 멎은 것처럼 보이는 구간이 거기다.

### 전경 배치의 기하 — 왜 눈대중으로 안 맞나

원근 카메라에서 거리 `d`에 있는 것이 화면에 담기는 세로 크기는 `2·d·tan(FOV/2)`. FOV 60이면 `1.155 × d`다. 리그는 `offset2D = (1, 0.75, -10)`이므로 플레이 평면은 11.55, 전경(Z=-4)은 6.93이 담긴다.

**가라앉음.** 카메라 축보다 아래에 있는 것을 카메라 쪽으로 당기면 화면 바깥으로 밀려 내려간다. 같은 월드 높이의 풀이 플랫폼보다 4% 아래에 찍혀 **덮이기는커녕 바닥에 파묻힌 것처럼 보인다.**

> 앞으로 Δ만큼 낼 때마다 월드 Y를 **`0.075 × Δ`** 올린다.
> (계수 0.075 = 카메라 높이 0.75 ÷ 거리 10 = 표면을 내려다보는 각의 탄젠트)

**가까울수록 어렵다.** 전경 높이 `a`가 플레이 평면의 높이 `H`까지 덮는다: `H = 0.75 − (10/d)·(0.75 − a)`.

| 전경 거리 | 표면에 겨우 닿음 | 캐릭터(2)를 다 덮음 | 쓸 수 있는 폭 |
|---:|---:|---:|---:|
| d=4 | 0.45 | 1.25 | **0.80** |
| d=6 | 0.30 | 1.50 | 1.20 |
| d=8 | 0.15 | 1.75 | **1.60** |

연출을 위해 카메라 가까이 당기면 "아무것도 안 덮음"과 "캐릭터까지 덮음" 사이가 0.8유닛으로 좁아진다. 손으로 맞추기 어려웠던 것이 감이 아니라 이 폭 때문이다.

**`ForegroundProjector`는 이 계산을 통째로 없앤다.** 카메라 C 기준으로 `P' = C + (P − C)·k`로 축소하면 P'는 같은 광선 위에 있으므로 **화면상 정확히 같은 자리에 찍힌다.** 높이 보정이 자동으로 포함된다(실측: 5개 지점 화면 오차 0.000000, 보정으로 올라간 Y = 0.150 = `0.075 × 2`). 정합은 기준 카메라 위치에서만 정확하고 카메라가 움직이면 시차로 어긋나는데, 그게 목적이다.

`ForegroundBand`는 성격이 다르다. 어떤 오브젝트에도 붙지 않고 레벨 X를 가로지르며, 가리는 것이 아니라 **스쳐 지나가는** 것이 목적이다. 밑동은 프레임 아래로 묻고 끝만 걸치게 둔다.

| Z 중심 | 시차 배속 | 프레임 하단 Y | 높이 2 풀이 먹는 화면 |
|---:|---:|---:|---:|
| −2 | 1.25× | −3.47 | 22% |
| **−4** | **1.67×** | **−2.31** | **29%** |
| −7 | 3.33× | −0.58 | 58% |

시차 배속이 "지나가는" 느낌의 세기다. −7쯤 되면 확 스쳐가는 대신 화면의 58%를 먹고 눈이 피로하다.

### 여기서 발이 걸리는 것

**`Physics.autoSyncTransforms`가 꺼져 있다.** 에디터에서 대상을 막 옮기거나 스케일한 직후면 콜라이더가 아직 물리 쪽에 반영되지 않아 `ClosestPoint`·`Raycast`가 **예전 위치로 답한다.** `PrefabFiller`의 콜라이더 내부 판정이 315개 중 27개만 통과했던 것이 이것이다. 물리를 묻기 전에 `Physics.SyncTransforms()`를 부른다.

**크기가 같은 플랫폼이 여럿이면 시드를 흩어야 한다.** Deck 12개가 전부 `6.95 × 0.42 × 4.93`이라 시드가 같으면 Poisson 결과가 동일해져 **완전히 똑같은 배치가 12벌** 깔린다. `targetIndex * 7919`를 더한다. 같은 선택이면 여전히 같은 결과가 나오므로 재현성은 유지된다.

**`Y Offset`의 뜻이 면마다 다르다.** Top 면에서는 높이(`bounds.max.y + YOffset`)지만 **Back 면에서는 깊이**(`bounds.min.z − YOffset`)다. 플랫폼 앞에 스커트를 깔 때 Back을 고르면 높이 보정 손잡이가 없다 — **Top에 `zOffset`으로 앞으로 미는 것이 맞다.**

**부모와 자식을 함께 선택하면 두 번 뿌려진다.** 다중 선택은 `IsChildOf`로 걸러 부모만 남긴다.

**`Foot`의 착지 판정은 인스펙터 마스크를 안 보고 있었다.** `1<<3`이 하드코딩되어 `_ground`를 바꿔도 반영되지 않았다. `RayCaster.Cast`도 `layerMask` 인자를 받아놓고 `Physics.Raycast`에 넘기지 않아, 보스 지면 판정이 전 레이어를 때리고 있었다.

---

## 에디터 툴 — 배칭과 메시 병합 <sub>담당: Claude Opus 5</sub>

### 먼저 볼 것은 병합이 아니다

같은 메시가 수백 개 깔린 상황에서 손이 먼저 가는 것은 메시 병합이지만, **비용 순서가 반대다.**

| | 라이트맵 재베이크 | 프리팹 연결 | 컬링 | 되돌리기 |
|---|---|---|---|---|
| GPU Resident Drawer | 불필요 | 유지 | 오히려 향상 | 체크박스 |
| 메시 병합 | **필요** | **끊김** | 격자 단위로 거칠어짐 | 원본 복구 |

그래서 `PC_RPAsset.asset`의 **GPU Resident Drawer를 켜 두었다**(`m_GPUResidentDrawerMode: 1`, `m_GPUResidentDrawerEnableOcclusionCullingInCameras: 1`). 활성 RP 애셋은 이것 하나이고 품질 레벨별 오버라이드가 없어 `Mobile_RPAsset`은 건드리지 않았다. `m_SmallMeshScreenPercentage`는 별개 최적화라 0(끔)으로 두었다 — 한 번에 둘을 바꾸면 무엇이 효과를 냈는지 알 수 없다.

> **아직 측정하지 않았다.** 켜기만 했고 프레임을 재지 못했다. DOTS instancing을 지원하지 않는 머티리얼은 **조용히 기존 경로로 빠지므로**, 드로우콜이 실제로 줄었는지 Frame Debugger로 확인해야 한다. `Sun_Temple` 계열은 빌트인이라 애초에 대상이 아니다. 빌드에서만 죽는다면 BatchRendererGroup 변형 스트리핑을 본다.

**프리팹 하나로 묶는 것은 런타임에 효과가 없다.** 렌더링 비용은 렌더러 단위이고 계층 구조는 무관하다. 씬 파일 크기·머지 충돌·하이어라키 정리에는 효과가 크므로, 그쪽이 목적이면 별개로 하면 된다.

### `Tools/Batching Sweeper`

탭 둘. **GPU 인스턴싱**은 인스턴싱이 이득인 머티리얼을 찾아 켜고, **정적 플래그**는 씬 렌더러를 출처 프리팹별로 묶어 종류 단위로 플래그를 건다.

인스턴싱 후보에서 빼는 조건이 판단의 전부다. 스킨드 메시는 본 스키닝이 인스턴스마다 달라 대상이 아니고, **정적 배칭이 걸린 렌더러도 뺀다** — 정적 배칭이 우선하므로 켜봐야 무시된다. 그리고 **렌더러 하나만 쓰는 머티리얼은 뺀다.** 인스턴싱은 중복이 있어야 이득이다.

셰이더의 인스턴싱 지원 여부는 **판정하지 않는다.** 유니티가 공개 API로 주지 않아 `ShaderUtil` 내부 리플렉션이 필요한데, 그건 에디터 버전이 오르면 조용히 죽는 종류의 의존이다(→ `PlaySoundEditorWindow`가 그렇게 쓰고 있다). 지원하지 않는 셰이더에서는 이 플래그가 에러 없이 무시되므로 해가 없다.

> 클래스와 파일 이름은 아직 `GpuInstancingSweeper`다. 창이 배칭 전반을 다루게 되면서 어긋났는데, 에디터를 켠 채 파일을 리네임하는 것이 위험해 미뤄 두었다.

### `Tools/Static Mesh Merger`

(메시 + 머티리얼 + 레이어 + 정적 플래그)가 같은 것끼리 묶고, 그 안에서 다시 **공간 격자**로 쪼개 합친다.

전부 하나로 합치지 않는 이유가 이 툴의 설계 전부다. 1500개를 메시 하나로 만들면 화면 구석에 한 포기만 걸쳐도 **통째로 그려져 컬링이 죽는다.** 셀 크기가 조절 손잡이다 — 작을수록 컬링이 잘 듣고, 클수록 개수가 더 준다.

**콜라이더가 있으면 건드리지 않는다.** 자기 자신이든 자식이든 하나라도 있으면 후보에서 뺀다. 합치면 콜라이더와 메시의 대응이 끊어진다. LOD 그룹도 뺀다 — 레벨을 섞어 합치면 LOD가 깨진다.

원본은 기본적으로 **지우지 않고 비활성으로만** 둔다. 라이트맵을 다시 구워야 하고 프리팹 연결이 끊기므로, 결과를 보고 되돌릴 여지를 남긴다.

> **병합 결과가 제자리에 오는지 에디터에서 확인하지 못했다.** 검증하려던 시점에 Unity MCP 응답이 반복해서 끊겼다. 코드는 표준 2패스 `CombineMeshes`(서브메시별로 먼저 합치고 다시 합치기)에 `worldToLocal * localToWorldMatrix`로 셀 원점 기준 변환을 넣은 것이지만, **실행으로 확인된 바는 없다.** 처음 쓸 때 원본 삭제를 끈 채 작은 묶음 하나로 먼저 대조할 것.

---

## 식생 · 바람 <sub>담당: Claude Opus 5</sub>

심어둔 나무·잔디가 멎어 보일 때, 원인은 계통에 따라 전혀 다른 곳에 있다. **두 계통이 서로 다른 방식으로 흔들리기 때문이다.**

| 계통 | 바람이 어디서 오나 | 정적 배칭 |
|---|---|---|
| ANGRY MESH (ASP) — 잔디·수피·나뭇잎 | **씬에 놓인 `ASP_Global_Settings` 하나**가 매 프레임 전역 셰이더 값을 쏜다 | 깨진다 |
| TidalFlask 폴리지 | 머티리얼 자체 파라미터. 월드 좌표와 버텍스 컬러로만 흔든다 | 안전하다 |

### ASP는 드라이버가 없으면 정확히 0만큼 흔들린다

ASP 셰이더는 변위를 `lerp(0, 바람, ASPW_WindToggle)`로 끝낸다. 그 전역값을 채우는 곳은 `ASPGlobalWind.Update()` 하나뿐이고, 그 컴포넌트가 씬에 없으면 값은 0이다 — 진폭들도 함께 0이다.

**머티리얼의 `_ENABLEWIND_ON`이 켜져 있어도 소용없다.** 스위치가 꺼지는 것이 아니라 곱해져 사라지는 것이라, 머티리얼을 열어봐도 이상한 곳이 보이지 않는다. 프리팹도 셰이더도 멀쩡한데 화면만 정지해 있으면 **머티리얼보다 먼저 씬에 드라이버가 있는지 본다.**

프리팹은 `Art/External/ANGRY MESH/Common/ASP Global Settings/`에 있다. **위치는 아무 의미가 없고 회전만 의미가 있다** — 오브젝트의 `forward`가 곧 바람 방향이다. 달려 있는 화살표 메시는 플레이 모드에서 스스로 숨지만 에디트 모드에서는 보이므로, 주인공이 서는 자리에 두면 캐릭터를 관통해 겹쳐 보인다.

현재 배경 씬의 상태:

| 배경 씬 | ASP 식생 | 드라이버 |
|---|---|---|
| 2. Floating Gardens | 많다 | 있다 |
| 5. Ochyra Shop | 있다 | 있다 |
| 4. the Depths | 거의 없다 | 있다 |
| 1. Temple of Awakening | 있다 | **없다** |
| 3. Sky Temple | 많다 | **없다** |
| 6. Old Cribs | 있다 | **없다** |
| Boss Fights/Ochyra | 가장 많다 | **없다** |

아래 넷은 심어둔 식생이 통째로 멎어 있다. 프리팹을 하나 넣으면 끝나지만, **바람 방향은 스테이지마다 사람이 정할 것**이라 임의로 채워두지 않았다.

### 정적 배칭과 메시 병합이 ASP 바람을 깬다

ASP는 **오브젝트 공간**에서 흔든다. 제 피벗을 축으로 돌리고(`RotateAroundAxis(0, positionOS, …)`), 개체마다 다른 위상을 오브젝트 행렬(`UNITY_MATRIX_M._m03_m13_m23`)에서 뽑는다. 둘 다 "이 메시에는 제 오브젝트가 있다"는 전제 위에 서 있다.

정적 배칭은 메시를 배치 루트 공간으로 합치면서 그 전제를 지운다. 그러면 **모든 포기가 같은 박자로, 제 밑동이 아니라 멀리 있는 루트를 축으로 돈다** — 흔들리는 대신 늘어난다.

그런데 **환경 배치 창의 기본 정적 플래그에 `Batching Static`이 들어 있다**(`EnvironmentPlacerEditor.cs:46`). 창은 GPU 인스턴싱과의 충돌만 경고하고 정점 애니메이션은 경고하지 않는다. ASP 식생을 뿌릴 때는 플래그에서 직접 빼야 한다.

**`Static Mesh Merger`는 한 단계 더 위험하다.** 병합 후보 조건이 곧 "`Batching Static`이 걸린 것"이어서(`StaticMeshMerger.cs:359`) 위에서 그렇게 뿌린 식생이 그대로 후보가 되고, 병합은 플래그와 달리 **메시에 구워져 되돌릴 수 없다.** 정점으로 흔드는 것은 병합하지 않는다.

TidalFlask 쪽은 월드 좌표와 버텍스 컬러만 쓰므로 배칭에도 병합에도 영향을 받지 않는다. **두 계통을 같은 규칙으로 다루지 않는다.**

### 멎어 보인다고 고장이 아니다

Scene 뷰의 **Always Refresh**가 꺼져 있으면 셰이더 `_Time`이 흐르지 않아 정점 애니메이션이 전부 정지 화면이 된다(→ *함께 지키는 것*).

그리고 **에디터에 열려 있는 씬은 디스크의 파일과 다를 수 있다.** 한 번은 열린 씬에 드라이버가 없는 것을 보고 "씬에 없다"고 단정했는데, 커밋된 파일에는 처음부터 들어 있었다. 없다고 말하기 전에 씬 파일을 직접 훑는다.

---

## HUD · 인스펙터로 엮는 UI 부품 <sub>담당: Claude Opus 5</sub>

위의 *UI · 대사 흐름*이 **바깥과 UI를 잇는 통로**를 다룬다면, 이 절은 **화면에 실제로 그려지는 것들**과 그것을 연출에 엮는 부품을 다룬다.

### 캔버스는 층으로 나눈다

| 층 | 무엇 | 어디 |
|---|---|---|
| `0` | HUD · 대사 | `UI Canvas` 프리팹 |
| `100` | 보스 체력바 | 스테이지 씬 |
| `1000` | 전환 커튼 | `UI - General` |

**같은 값을 두면 안 된다.** Screen Space-Overlay 캔버스끼리 `sortingOrder`가 같으면 누가 위에 그려질지 정해져 있지 않다 — 씬 로드 순서에 따라 갈린다. 커튼이 버튼 밑에 깔리면 화면을 덮어야 할 것이 안 덮인다.

스테이지가 **자기 UI를 들고 오기 때문에** 층이 필요하다. 보스 체력바를 100에 둔 것은 HUD보다 위, 커튼보다 아래라는 뜻이다.

### 보스 체력바 — 화면 쪽

`BossHealthBar`는 **늘 켜져 있고**, 껐다 켜는 것은 자식 `Bar`다. 자기를 끄면 `OnDisable`에서 구독이 끊겨 보스가 깨어나도 다시 켜줄 사람이 없다.

`Bar`를 **화면 전체 크기로** 두고 그 안에 `Panel`(하단 그라데이션)과 `Track`(게이지)을 나란히 뒀다. 게이지 크기의 작은 사각형 안에는 전체 폭 패널이 들어가지 않고, 무엇보다 **패널이 게이지와 함께 켜지고 꺼져야** 하기 때문이다. 보스가 없을 때 빈 그라데이션만 깔리면 안 된다.

패널은 `Main/Footer/Footer Panel`을 그대로 옮긴 것이다 — `gradation.png`, pivot을 위로 두고 **`scaleY = -1`로 뒤집어 올린다.** 그냥 두면 화면 아래로 뻗어 보이지 않는다. 투명도는 색이 아니라 텍스처가 갖고 있어서, 색을 흰색으로 두어야 Footer와 같은 농도가 나온다.

바는 스테이지 씬에 산다. 스테이지가 내려가면 함께 사라지는 것이 맞기 때문이다.

> 지금 Semprea·Ochyra에 **같은 구조가 손으로 두 벌** 들어가 있다. 스타일을 바꿀 일이 생기면 프리팹 하나로 묶는 편이 낫다 — 스테이지마다 다른 것은 `_vm`과 이름표뿐이다.

### 전투 이벤트로 뺀 이유

`BossManager.onCombatStart` / `onCombatEnd`. 처음에는 `Boss_Idle`이 `BossHealthPresenter`를 직접 불렀는데, 그러면 **보스 FSM이 UI를 알게 되고** 체력바를 두지 않은 보스마다 예외가 하나씩 생긴다.

이벤트로 빼면 상태 기계는 "전투가 열렸다"만 말하고 **무엇이 듣는지 모른다.** 음악이든 관문 잠그기든 같은 자리에 물릴 수 있다. 발화 지점은 그대로 `Boss_Idle.ToAlert()`와 `Boss_Dead.Enter()`다 — 화면 모드를 바꾸는 그 짝과 정확히 겹친다.

### 남은 회복 횟수 (`HealChargeBar`)

칸을 **런타임에 만들지 않고 인스펙터에 authoring한다.** 개수가 거의 바뀌지 않는데 만들어 붙이는 코드를 두면 레이아웃을 눈으로 맞출 수 없게 된다. 칸 수와 `HealChargeMax`가 어긋나면 한 번 경고한다.

프레임은 `anteriorEdge`, 채움은 `Square`. **`anteriorEdge`는 사방 10px 보더가 잡힌 9-slice 스프라이트라 `Sliced`로 넣는다** — 칸 크기를 바꿔도 테두리 두께가 유지된다. 채움은 보더가 없어 `Simple`이고, 프레임을 나중 형제로 두어 채움 위에 그려지게 한다.

**값이 바뀔 때만 다시 그린다.** `HPbar`가 매 프레임 칠하는 것은 그쪽이 시간 배속까지 함께 계산하는 사정이 있어서고, 이쪽은 그럴 이유가 없다.

캐릭터는 런타임에 찾는다(`PlayerManager.instance.player.root`). 없으면 조용히 지나간다.

### 연출에 엮는 발동 부품 셋

세 가지 모두 **씬 바깥을 가리키는 참조가 없다.** 프리팹으로 떼어 다른 씬에 옮겨도 끊길 자리가 없다.

`RaiseInvoke` — '일어나기' 키를 누르면 이벤트. **키보드를 직접 읽지 않고 `InputManager.move_raise` 액션을 듣는다.** 리바인딩과 게임패드가 그대로 따라오고, 어느 키인지 아는 곳이 `InputManager` 하나로 남는다. 구독을 `OnEnable`과 `Start` **두 곳에서** 시도하는데, 씬이 올라오는 프레임에는 `InputManager.Awake`가 이쪽 `OnEnable`보다 늦을 수 있기 때문이다 — 오브젝트 사이 순서는 정해져 있지 않지만 모든 `Awake`는 모든 `Start`보다 먼저 돈다.

`TimedInvoke` — 정해둔 시간이 지나면 이벤트. **기본이 벽시계다.** 체력이 `TimeManager` 배속을 끌어내리므로 스케일 시계로 재면 빈사 상태에서 3초가 10초가 되고, 일시정지 중에는 영영 끝나지 않는다(→ 커튼이 벽시계를 쓰는 것과 같은 이유). 이벤트를 쏘기 **전에** 멈추는데, 이벤트가 `Begin()`을 다시 부르는 연출이면 뒤에서 멈출 경우 방금 건 요청을 스스로 꺼버리기 때문이다.

`UiVisibility` — UI 전체를 접었다 편다. **무엇을 접는지는 이쪽이 모른다.** `HudService`만 알고 이쪽은 "접어라/펴라"만 말하므로, 묶음이 늘어도 이 파일은 그대로다. 목록에서 `Central`(중앙 문구)과 `Flipbook`(대사창)은 일부러 뺐다 — 그 둘은 내용에 따라 스스로 여닫으므로, 넣으면 `FadeIn()`이 **빈 대사창을 띄운다.** `Esc Menu`도 뺐다. 스테이지 스크립트가 일시정지를 열어버리기 때문이다(`PauseGame`이 얹혀 있어 시간까지 멎는다).

### `UIGroupFader`에서 발이 걸리는 것

**`SetActivity`는 자기 자식이 아니라 목록에 든 그래픽의 GameObject를 끈다.** 계층상 전혀 다른 가지에 있는 오브젝트도 `_uiElements`에만 들어가면 함께 꺼진다. 그래서 "에디터에서는 멀쩡한데 재생하면 사라지는 오브젝트"가 생기면 **하이어라키가 아니라 페이더 목록부터** 봐야 한다. 실제로 Title의 `Canvas/Panel`이 엉뚱한 페이더 목록에 들어가 그렇게 사라졌다.

같은 성질을 `Esc Menu`가 활용한다 — 일시정지는 별도 배선 없이 페이더가 `Panel`을 `SetActive` 하는 것에 `PauseGame.OnEnable/OnDisable`이 얹혀 걸린다. **목록을 건드리면 시간 정지까지 함께 깨진다.**

**같은 상태로 다시 페이드하지 않는다.** 예전에는 `FadeOut`을 두 번 부르면 알파가 1로 튀었다가 다시 내려가 깜빡였다. `_visible`을 두어 상태가 바뀔 때만 돌게 했다. 표시 여부를 내용이 정하게 된 뒤로는(빈 문구면 숨김) 이 가드가 없으면 페이지를 넘길 때마다 깜빡인다.

**`_fadedOnStart`는 알파만이 아니라 `SetActive(false)`까지 한다.** 그리고 그것을 `Start`에서 하므로, 컴포넌트를 꺼두면 그 스냅이 통째로 무시된다 — 켜진 채로 시작한다. `FadeIn`/`FadeOut`은 `UnityEvent`로 직접 불리므로 disabled 상태에서도 정상 동작한다(`Awake`는 disabled여도 돌아 원본 알파는 잡혀 있다).

---

## 함께 지키는 것

**에디터가 켜져 있는 동안 유니티 애셋을 `rm`으로 지우지 않는다.** `PC_Renderer.asset`에서 렌더러 피처가 조용히 사라진다. 지워야 하면 `AssetDatabase.DeleteAssets`로 지운다.

**네임스페이스를 일부러 두지 않았다.** 에이전트가 네임스페이스 경계를 소유권 울타리로 읽고 남의 코드를 안 고치려 들기 때문이다. 대신 FSM의 초기 상태가 **AQN 문자열로 직렬화되어** 있어, 타입 이름을 바꾸면 씬이 조용히 깨진다 — `SerializedType.Resolve`를 쓰고 `Type.GetType`을 직접 부르지 않는다.

**`FreezePositionZ`는 2D 모드다.** 버그가 아니다.

**Sampler(External)는 외력 몸이다.** 거기서 수평 속도를 계산하지 않는다. Singular의 위치 권한을 건드리지 않는다.

**`AutoPlatform` 재생성은 파괴적이다.** 손으로 다듬은 것을 지우고도 "통과"라고 보고한다. 다시 돌리기 전에 물어본다.

**`DamagableBase`는 공유물이다.** 잡몹도 보스도 물려받으므로 플레이어 전용 로직을 넣지 않는다.

**에디터 창이 포커스를 잃으면 틱이 멈춘다.** 에디트 모드 `Update`와 셰이더 `_Time`이 함께 멎는다. 전역값이 0으로 찍혔다고 고침이 깨진 증거가 되지 않는다.

**`MoveTo`는 참조가 0인 채로 남겨둔 저작 도구다.** 죽은 코드가 아니다.

---

## 임시로 남아 있는 것

`Assets/Scripts/_Trace/LedgeDropTrace.cs` — 턱 잡기 진단용. 판이 시작될 때 스스로 붙어 `%TEMP%\ledge-drop-trace*.txt`에 기록한다. **원인을 다 잡았으므로 폴더째 지워도 된다.** 참조하는 코드가 없다.

---

## 아직 적히지 않은 파트

담당하신 분이 채워 주십시오.

- 사운드
